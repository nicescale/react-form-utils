{
  "name": "@csphere/react-form-utils",
  "version": "1.2.0",
  "description": "Form utils for react",
  "main": "lib/index.js",
  "directories": {
    "lib": "lib/"
  },
  "peerDependencies": {
    "react": ">=0.13.1 || ^0.14.0-rc1",
    "immutable": ">=3.x.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf lib && babel src --out-dir lib"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/nicescale/react-form-utils.git"
  },
  "author": "tsing@nicescale.com",
  "bugs": {
    "url": "https://github.com/nicescale/react-form-utils/issues"
  },
  "homepage": "https://github.com/nicescale/react-form-utils",
  "devDependencies": {
    "babel": "^5.8.23"
  }
}
